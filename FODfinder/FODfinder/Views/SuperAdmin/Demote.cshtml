@model FODfinder.Models.ApplicationUser
@{
    ViewBag.Title = "Demote User";
}

<div class="container-fluid pt-4">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="card shadow">
                <div class="card-header bg-secondary text-gray text-center">
                    <h4>Demote User</h4>
                </div>
                <div class="card-body">
                    @using (Html.BeginForm("Demote", "SuperAdmin", FormMethod.Post, new { @class = "container-fluid p-2" }))
                    {
                        @Html.HiddenFor(model => model.Id)
                        <h5 class="text-center">Are you sure you want to demote @Html.DisplayFor(model => model.UserName) from Admin?</h5>
                        <div class="input-group pt-3">
                            <button type="submit" class="btn btn-primary ml-auto p-2" id="PromoteDemoteForm">Demote</button>
                            <div class="p-2"></div>
                            @Html.ActionLink("Nevermind", "Index", null, new { @class = "btn btn-primary mr-auto p-2", id = "PromoteDemoteForm" })
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

